---
description: Update the Pravex plugin to the latest version
allowed-tools: Bash(node:*)
---

Update the Pravex plugin.

**Run this and show its output verbatim:**

`node "${CLAUDE_PLUGIN_ROOT}/scripts/update.js"`

If it says the plugin was updated, tell the user to run `/reload-plugins` to use
the new version in this session. Do not run anything else.
